startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}  # 기본 설정에서는 추가 설정 필요 없음
  commandFunction: |
    (config) => ({ 
      command: 'python', 
      args: ['server.py'],
      env: {} 
    })